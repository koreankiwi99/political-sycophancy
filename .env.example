# Copy to .env and fill in. Loaded by python-dotenv.

# OpenRouter key — used by the generation pipeline and the scoring judge.
OPENROUTER_API_KEY=

# Scoring judge model (results/.../score.py). Optional; defaults to openai/gpt-4o.
# JUDGE_MODEL=openai/gpt-4o
